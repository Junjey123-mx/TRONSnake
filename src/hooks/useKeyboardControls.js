// TODO: Implement keyboard direction controls
// Parameter:
//   onDirectionChange (fn) — called with a DIRECTIONS value when a valid key is pressed
export function useKeyboardControls(_onDirectionChange) {
  // TODO: useEffect(() => {
  //   const handleKeyDown = (e) => {
  //     const map = { ArrowUp: DIRECTIONS.UP, ArrowDown: DIRECTIONS.DOWN, ... };
  //     const dir = map[e.key];
  //     if (dir) onDirectionChange(dir);
  //   };
  //   window.addEventListener('keydown', handleKeyDown);
  //   return () => window.removeEventListener('keydown', handleKeyDown);
  // }, [onDirectionChange]);
}
