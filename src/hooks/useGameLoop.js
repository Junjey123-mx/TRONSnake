// TODO: Implement game loop using setInterval
// Parameters:
//   isRunning (bool)  — whether the loop should be active
//   speed     (ms)    — interval delay between ticks
//   onTick    (fn)    — callback called on each tick
export function useGameLoop(_isRunning, _speed, _onTick) {
  // TODO: useEffect(() => {
  //   if (!isRunning) return;
  //   const id = setInterval(onTick, speed);
  //   return () => clearInterval(id);
  // }, [isRunning, speed, onTick]);
}
