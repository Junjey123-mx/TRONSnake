// Food is rendered directly by Board.jsx via CSS Grid.
// This component is prepared for future independent rendering if needed.

function Food({ food }) {
  if (!food) return null
  // TODO: Render food independently when decoupled from Board
  return null
}

export default Food
